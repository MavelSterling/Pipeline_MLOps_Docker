# Arquitectura Técnica Detallada - Sistema de Diagnóstico Médico MLOps

## Vista General de la Arquitectura

Este documento presenta la arquitectura técnica completa del sistema, con diagramas detallados, especificaciones de componentes y flujos de datos.

---

## 🏗️ Arquitectura de Referencia

### **Capas de la Arquitectura**

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CAPA DE PRESENTACIÓN                         │
├─────────────────────────────────────────────────────────────────────┤
│  Web App  │  Mobile App  │  EHR Integration  │  Desktop App (Local) │
└────┬──────┴──────┬───────┴──────────┬────────┴──────────┬───────────┘
     │             │                  │                   │
     └─────────────┴──────────────────┴───────────────────┘
                              │
┌─────────────────────────────┼─────────────────────────────────────┐
│                    CAPA DE API GATEWAY                             │
├────────────────────────────────────────────────────────────────────┤
│           Azure API Management                                     │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │ • OAuth2 / JWT Authentication                             │     │
│  │ • Rate Limiting (100 req/min)                             │     │
│  │ • Request/Response Transformation                         │     │
│  │ • Caching (1 hour)                                        │     │
│  │ • IP Whitelisting                                         │     │
│  │ • Analytics & Monitoring                                  │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────┬───────────────────────────────────────┘
                             │
┌────────────────────────────┼───────────────────────────────────────┐
│                    CAPA DE APLICACIÓN                              │
├────────────────────────────────────────────────────────────────────┤
│                 Azure Kubernetes Service (AKS)                     │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │  FastAPI   │  │  FastAPI   │  │  FastAPI   │  │   ...      │  │
│  │   Pod 1    │  │   Pod 2    │  │   Pod 3    │  │  Pod N     │  │
│  │            │  │            │  │            │  │            │  │
│  │ • /predict │  │ • /predict │  │ • /predict │  │ • /predict │  │
│  │ • /health  │  │ • /health  │  │ • /health  │  │ • /health  │  │
│  │ • /metrics │  │ • /metrics │  │ • /metrics │  │ • /metrics │  │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘  │
│       ↕                 ↕                 ↕               ↕        │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │       NGINX Ingress Controller (Load Balancer)            │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │   Horizontal Pod Autoscaler (HPA)                         │     │
│  │   Min: 3 pods | Max: 20 pods                              │     │
│  │   Targets: CPU 70%, Memory 80%                            │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────┬───────────────────────────────────────┘
                             │
┌────────────────────────────┼───────────────────────────────────────┐
│                    CAPA DE LÓGICA DE ML                            │
├────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              MLflow Model Registry                        │     │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │     │
│  │  │ Ensemble    │  │ Enf.Comunes │  │ Enf.Huérfanas│     │     │
│  │  │   Model     │  │   Model     │  │   Model     │      │     │
│  │  │  (v2.3)     │  │  (v1.5)     │  │  (v1.2)     │      │     │
│  │  │ Production  │  │ Component   │  │ Component   │      │     │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              Feature Store (Databricks)                   │     │
│  │  • Online Store (low latency <50ms)                       │     │
│  │  • Offline Store (training data)                          │     │
│  │  • 150+ features (comunes) + 50+ features (huérfanas)     │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              SHAP Explainer                               │     │
│  │  • TreeExplainer para modelos basados en árboles          │     │
│  │  • KernelExplainer para modelos complejos                 │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────┬───────────────────────────────────────┘
                             │
┌────────────────────────────┼───────────────────────────────────────┐
│                    CAPA DE DATOS                                   │
├────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────┐  ┌───────────────────────┐             │
│  │ Azure Data Lake Gen2  │  │   Azure Cosmos DB     │             │
│  │ ┌─────────────────┐   │  │ ┌─────────────────┐   │             │
│  │ │ Bronze (Raw)    │   │  │ │ Predictions     │   │             │
│  │ │ Silver (Cleaned)│   │  │ │ Feedback        │   │             │
│  │ │ Gold (Features) │   │  │ │ Audit Logs      │   │             │
│  │ └─────────────────┘   │  │ └─────────────────┘   │             │
│  │   Delta Lake Format   │  │   Multi-region        │             │
│  └───────────────────────┘  └───────────────────────┘             │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              Azure Event Hubs (Streaming)                 │     │
│  │  • Topic: medical-symptoms-stream                         │     │
│  │  • Throughput: 1000 events/sec                            │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────┬───────────────────────────────────────┘
                             │
┌────────────────────────────┼───────────────────────────────────────┐
│                    CAPA DE PROCESAMIENTO                           │
├────────────────────────────────────────────────────────────────────┤
│                        Databricks Workspace                        │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  Apache Spark Clusters                                    │     │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │     │
│  │  │ ETL Cluster │  │Training GPU │  │ Inference   │      │     │
│  │  │ (CPU)       │  │ Cluster     │  │ Batch       │      │     │
│  │  │ 4 nodes     │  │ 2 nodes GPU │  │ 2 nodes     │      │     │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              Azure Data Factory                           │     │
│  │  Pipeline: medical-data-ingestion                         │     │
│  │  • Copy Activity (EHR → Data Lake)                        │     │
│  │  • DataFlow (transformaciones básicas)                    │     │
│  │  • Trigger: Schedule (hourly) + Event-based              │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────┬───────────────────────────────────────┘
                             │
┌────────────────────────────┼───────────────────────────────────────┐
│                    CAPA DE MONITOREO                               │
├────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────┐               │
│  │   Prometheus         │  │   Grafana            │               │
│  │   • Scraping cada    │→ │   • 3 Dashboards     │               │
│  │     15 segundos      │  │   • Alertas visuales │               │
│  │   • Retención 30d    │  │   • Drill-down       │               │
│  └──────────────────────┘  └──────────────────────┘               │
│           ↑                         ↑                              │
│  ┌────────┴──────────────────────┬─────────────────────┐          │
│  │ Application Insights          │ Azure Monitor        │          │
│  │ • Telemetría aplicación       │ • Métricas infra     │          │
│  │ • Distributed tracing         │ • VM/AKS metrics     │          │
│  └────────────────────────────────┴────────────────────┘          │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              Evidently AI (ML Monitoring)                 │     │
│  │  • Data Drift Detection (daily)                           │     │
│  │  • Model Performance Monitoring                           │     │
│  │  • Test Suites automáticos                                │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              Azure Log Analytics                          │     │
│  │  • Logs centralizados de todos los servicios             │     │
│  │  • Kusto Query Language (KQL) para análisis              │     │
│  │  • Retención 90 días (configurable)                       │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────┬───────────────────────────────────────┘
                             │
┌────────────────────────────┼───────────────────────────────────────┐
│                    CAPA DE SEGURIDAD                               │
├────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────┐               │
│  │ Azure Active         │  │ Azure Key Vault      │               │
│  │ Directory            │  │ • Secretos           │               │
│  │ • SSO                │  │ • Certificados SSL   │               │
│  │ • MFA                │  │ • Connection strings │               │
│  │ • RBAC (4 roles)     │  │ • Rotación auto      │               │
│  └──────────────────────┘  └──────────────────────┘               │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              Azure Policy & Compliance                    │     │
│  │  • HIPAA Compliance policies                              │     │
│  │  • GDPR data protection rules                             │     │
│  │  • Audit log enforcement                                  │     │
│  │  • Encryption at rest/transit requirements                │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │              Azure Security Center                        │     │
│  │  • Threat detection                                       │     │
│  │  • Vulnerability assessment                               │     │
│  │  • Security recommendations                               │     │
│  └──────────────────────────────────────────────────────────┘     │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Datos End-to-End

### **1. Flujo de Entrenamiento (Batch - Offline)**

```
┌──────────────────────────────────────────────────────────────────────┐
│                      ENTRENAMIENTO DE MODELOS                         │
└──────────────────────────────────────────────────────────────────────┘

1️⃣ INGESTA
┌─────────────┐
│   EHR / HL7 │
│   Sistemas  │
│  Hospitalarios│
└──────┬──────┘
       │
       ↓ (Azure Data Factory - Schedule: Hourly)
┌──────────────────┐
│ Azure Data Lake  │
│ Bronze Layer     │
│ (Parquet, Raw)   │
└──────┬───────────┘
       │
2️⃣ LIMPIEZA Y VALIDACIÓN
       ↓ (Databricks ETL Job)
┌──────────────────┐
│ Limpieza de datos│
│ • Normalización  │
│ • Deduplicación  │
│ • Imputación     │
└──────┬───────────┘
       │
       ↓ (Great Expectations validation)
┌──────────────────┐
│ Azure Data Lake  │
│ Silver Layer     │
│ (Delta, Cleaned) │
└──────┬───────────┘
       │
3️⃣ FEATURE ENGINEERING
       ↓ (Databricks Feature Engineering Job)
┌──────────────────┐
│ Generación       │
│ features         │
│ • Encoding       │
│ • Aggregations   │
│ • Embeddings     │
└──────┬───────────┘
       │
       ↓ (Write to Feature Store)
┌──────────────────┐   ┌──────────────────┐
│ Azure Data Lake  │   │ Feature Store    │
│ Gold Layer       │→  │ (Databricks)     │
│ (Delta, Features)│   │ Offline Store    │
└──────────────────┘   └──────┬───────────┘
                              │
4️⃣ ENTRENAMIENTO
                              ↓ (Read features)
                       ┌──────────────────┐
                       │ Databricks GPU   │
                       │ Training Job     │
                       │ • XGBoost        │
                       │ • Optuna tuning  │
                       │ • Cross-val      │
                       └──────┬───────────┘
                              │
                              ↓ (MLflow logging)
                       ┌──────────────────┐
                       │ MLflow           │
                       │ Experiments      │
                       │ • Params         │
                       │ • Metrics        │
                       │ • Artifacts      │
                       └──────┬───────────┘
                              │
5️⃣ VALIDACIÓN
                              ↓
                       ┌──────────────────┐
                       │ Model Validation │
                       │ • Unit tests     │
                       │ • Integration    │
                       │ • Performance    │
                       │ • Fairness       │
                       └──────┬───────────┘
                              │
                              ↓ (Manual approval)
                       ┌──────────────────┐
                       │ Validación       │
                       │ Médica           │
                       │ (5+ doctores)    │
                       └──────┬───────────┘
                              │
6️⃣ REGISTRO
                              ↓ (Promote to registry)
                       ┌──────────────────┐
                       │ MLflow Model     │
                       │ Registry         │
                       │ Stage: Staging   │
                       └──────┬───────────┘
                              │
7️⃣ CI/CD
                              ↓ (Azure DevOps Pipeline)
                       ┌──────────────────┐
                       │ Build Docker     │
                       │ Image            │
                       └──────┬───────────┘
                              │
                              ↓ (Push to ACR)
                       ┌──────────────────┐
                       │ Azure Container  │
                       │ Registry         │
                       └──────┬───────────┘
                              │
8️⃣ DESPLIEGUE
                              ↓ (Deploy to AKS)
                       ┌──────────────────┐
                       │ AKS Staging      │
                       │ Namespace        │
                       └──────┬───────────┘
                              │
                              ↓ (A/B Test 24h)
                       ┌──────────────────┐
                       │ A/B Testing      │
                       │ 10% traffic      │
                       └──────┬───────────┘
                              │
                              ↓ (If successful)
                       ┌──────────────────┐
                       │ AKS Production   │
                       │ Namespace        │
                       │ Status: Serving  │
                       └──────────────────┘
```

---

### **2. Flujo de Inferencia (Online - Real-time)**

```
┌──────────────────────────────────────────────────────────────────────┐
│                      PREDICCIÓN EN TIEMPO REAL                        │
└──────────────────────────────────────────────────────────────────────┘

1️⃣ REQUEST
┌──────────────┐
│ Médico       │
│ (Web/Mobile) │
└──────┬───────┘
       │ HTTPS POST /predict
       ↓
┌──────────────────┐
│ Azure API        │
│ Management       │
│ • Auth (JWT)     │
│ • Rate limit     │
│ • Cache check    │
└──────┬───────────┘
       │
       ↓ (If not cached)
┌──────────────────┐
│ NGINX Ingress    │
│ Load Balancer    │
└──────┬───────────┘
       │
       ↓ (Round-robin)
┌──────────────────┐
│ AKS Pod          │
│ FastAPI App      │
│ Pod #2 (example) │
└──────┬───────────┘
       │
2️⃣ PREPROCESSING
       ↓ (Input validation)
┌──────────────────┐
│ Pydantic         │
│ Validation       │
│ • Schema check   │
│ • Range check    │
└──────┬───────────┘
       │
       ↓ (Feature engineering)
┌──────────────────┐
│ Feature          │
│ Transformation   │
│ • Normalization  │
│ • Encoding       │
└──────┬───────────┘
       │
       ↓ (Fetch from Feature Store)
┌──────────────────┐
│ Feature Store    │
│ Online (Redis)   │
│ <50ms latency    │
└──────┬───────────┘
       │
3️⃣ PREDICCIÓN
       ↓ (Call model)
┌──────────────────┐
│ MLflow Model     │
│ (In memory)      │
│ Ensemble v2.3    │
└──────┬───────────┘
       │
       ↓ (Submodelos)
┌─────────────────────────────────────┐
│  ┌──────────┐    ┌──────────┐      │
│  │ Modelo   │    │ Modelo   │      │
│  │ Comunes  │    │ Huérfanas│      │
│  └────┬─────┘    └────┬─────┘      │
│       │               │             │
│       └───────┬───────┘             │
│               ↓                     │
│        ┌──────────────┐             │
│        │ Meta-learner │             │
│        │ (Agregación) │             │
│        └──────┬───────┘             │
└───────────────┼─────────────────────┘
                │
4️⃣ EXPLICABILIDAD
                ↓ (Generate explanation)
┌──────────────────┐
│ SHAP Explainer   │
│ • TreeExplainer  │
│ • Feature impact │
└──────┬───────────┘
       │
5️⃣ POST-PROCESSING
       ↓ (Format response)
┌──────────────────┐
│ Response Builder │
│ • Severity       │
│ • Confidence     │
│ • Explanation    │
│ • Recommendations│
└──────┬───────────┘
       │
6️⃣ LOGGING & MONITORING
       ├──→ (Async logging)
       │    ┌──────────────────┐
       │    │ Azure Cosmos DB  │
       │    │ • Prediction log │
       │    │ • Audit trail    │
       │    └──────────────────┘
       │
       ├──→ (Metrics export)
       │    ┌──────────────────┐
       │    │ Prometheus       │
       │    │ • Counter        │
       │    │ • Histogram      │
       │    └──────────────────┘
       │
7️⃣ RESPONSE
       ↓ (Return to client)
┌──────────────────┐
│ FastAPI Response │
│ Status: 200 OK   │
│ JSON payload     │
└──────┬───────────┘
       │
       ↓ (Cache for 1h)
┌──────────────────┐
│ API Management   │
│ Cache            │
└──────┬───────────┘
       │
       ↓ HTTPS Response
┌──────────────────┐
│ Médico           │
│ Recibe resultado │
│ con explicación  │
└──────────────────┘

⏱️ Total Latency Target: <500ms p99
   - API Gateway: ~20ms
   - Feature Store: ~50ms
   - Model inference: ~200ms
   - SHAP explanation: ~150ms
   - Logging (async): ~0ms
   - Network: ~80ms
```

---

## 🔄 Flujo de Reentrenamiento Automático

```
┌──────────────────────────────────────────────────────────────────────┐
│                    REENTRENAMIENTO CONTINUO                           │
└──────────────────────────────────────────────────────────────────────┘

TRIGGERS (cualquiera de estos)
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ Schedule         │  │ Data Drift       │  │ Performance      │
│ (Bi-weekly)      │  │ Alert            │  │ Degradation      │
│                  │  │ (drift >0.7)     │  │ (F1 drop >5%)    │
└────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                     │                     │
         └─────────────────────┴─────────────────────┘
                               │
                               ↓
                  ┌────────────────────────┐
                  │ Azure Logic Apps       │
                  │ Workflow Trigger       │
                  └────────┬───────────────┘
                           │
                           ↓ (Call Databricks API)
                  ┌────────────────────────┐
                  │ Databricks Job         │
                  │ "retrain-ensemble"     │
                  └────────┬───────────────┘
                           │
1️⃣ EXTRACCIÓN                │
                           ↓
┌────────────────────────────────────────────────────────────┐
│ Query new data since last training                         │
│ Source: Azure Data Lake (Silver Layer)                     │
│ Time range: [last_training_date, now]                      │
│ Expected: ~500-5000 new cases                              │
└────────────────────┬───────────────────────────────────────┘
                     │
2️⃣ VALIDACIÓN              ↓
┌────────────────────────────────────────────────────────────┐
│ Great Expectations validation                              │
│ • Schema consistency                                        │
│ • Value ranges                                              │
│ • Distribution checks                                       │
│ IF FAIL → Stop & Alert ML team                             │
└────────────────────┬───────────────────────────────────────┘
                     │
3️⃣ COMBINACIÓN            ↓
┌────────────────────────────────────────────────────────────┐
│ Combine new data with historical                           │
│ • Append to training set                                    │
│ • Re-balance classes (SMOTE if needed)                      │
│ • Update Feature Store                                      │
└────────────────────┬───────────────────────────────────────┘
                     │
4️⃣ ENTRENAMIENTO          ↓
┌────────────────────────────────────────────────────────────┐
│ MLflow Experiment: retrain_[timestamp]                     │
│ • Train on combined dataset                                 │
│ • Hyperparameter search (500 trials)                        │
│ • Cross-validation (5-fold)                                 │
│ • Log all metrics to MLflow                                 │
└────────────────────┬───────────────────────────────────────┘
                     │
5️⃣ EVALUACIÓN             ↓
┌────────────────────────────────────────────────────────────┐
│ Compare NEW model vs CURRENT model                         │
│                                                             │
│ Test set: Last 1000 cases (holdout)                        │
│                                                             │
│ Metrics comparison:                                         │
│ ┌──────────────┬──────────┬──────────┐                    │
│ │ Metric       │ Current  │ New      │                    │
│ ├──────────────┼──────────┼──────────┤                    │
│ │ F1-score     │ 0.88     │ 0.90     │ ✓ Better          │
│ │ Recall (crit)│ 0.95     │ 0.96     │ ✓ Better          │
│ │ Specificity  │ 0.82     │ 0.81     │ ✗ Worse (minor)   │
│ └──────────────┴──────────┴──────────┘                    │
│                                                             │
│ Decision: PROMOTE (F1 improvement + recall maintained)     │
└────────────────────┬───────────────────────────────────────┘
                     │
6️⃣ REGISTRO               ↓ (If decision = PROMOTE)
┌────────────────────────────────────────────────────────────┐
│ MLflow Model Registry                                       │
│ • Register new model                                        │
│ • Stage: Staging                                            │
│ • Version: v2.4 (auto-increment)                            │
│ • Tags: {retrain_trigger: "scheduled", date: "2024-01-15"}│
└────────────────────┬───────────────────────────────────────┘
                     │
7️⃣ NOTIFICACIÓN           ↓
┌────────────────────────────────────────────────────────────┐
│ Send notification to ML Team                                │
│ Channel: Microsoft Teams                                    │
│                                                             │
│ Message:                                                    │
│ "🎉 New model v2.4 ready for review!                       │
│  F1: 0.88 → 0.90 (+2.3%)                                   │
│  Please review and approve for A/B testing"                 │
└────────────────────┬───────────────────────────────────────┘
                     │
8️⃣ REVISIÓN MANUAL        ↓ (Human in the loop)
┌────────────────────────────────────────────────────────────┐
│ ML Engineer reviews:                                        │
│ • MLflow experiment details                                 │
│ • Confusion matrix                                          │
│ • Feature importance changes                                │
│ • Sample predictions                                        │
│                                                             │
│ Action: APPROVE for A/B testing                             │
└────────────────────┬───────────────────────────────────────┘
                     │
9️⃣ A/B TESTING            ↓ (Trigger CI/CD)
┌────────────────────────────────────────────────────────────┐
│ Azure DevOps Pipeline triggered                             │
│ • Build Docker image with new model                         │
│ • Deploy to AKS Staging namespace                           │
│ • Configure traffic split: 90% current, 10% new             │
│ • Monitor for 24 hours                                      │
│                                                             │
│ Metrics monitored:                                          │
│ • Latency (should not increase >10%)                        │
│ • Error rate (should not increase)                          │
│ • User feedback (médicos)                                   │
│ • Prediction distribution (drift check)                     │
└────────────────────┬───────────────────────────────────────┘
                     │
🔟 DECISIÓN FINAL         ↓ (After 24h)
┌────────────────────────────────────────────────────────────┐
│ Automated decision based on metrics:                        │
│                                                             │
│ IF new_model_metrics >= current_model_metrics:             │
│     Gradually increase traffic to new model:                │
│     10% → 25% → 50% → 75% → 100%                           │
│     Each step: 4 hours of monitoring                        │
│                                                             │
│     IF issues detected at any step:                         │
│         ROLLBACK to current model                           │
│         Alert ML team                                       │
│                                                             │
│ ELSE:                                                       │
│     ROLLBACK to current model                               │
│     Log reason in MLflow                                    │
│     Do NOT promote to production                            │
└────────────────────┬───────────────────────────────────────┘
                     │
                     ↓ (If successful)
┌────────────────────────────────────────────────────────────┐
│ MLflow Model Registry                                       │
│ • Transition: Staging → Production                          │
│ • Archive old model (v2.3 → Archived)                       │
│ • Update metadata:                                          │
│   - deployed_at: "2024-01-16T10:30:00Z"                     │
│   - deployment_method: "canary_release"                     │
└────────────────────────────────────────────────────────────┘

✅ REENTRENAMIENTO COMPLETO
   Total time: ~2-3 días (training + monitoring)
   Fully automated except manual approval step
```

---

## 📊 Especificaciones de Componentes

### **1. Azure Kubernetes Service (AKS)**

```yaml
# Configuración del cluster
cluster_name: medical-diagnosis-aks-prod
region: West US 2
kubernetes_version: 1.28

# Node pools
node_pools:
  - name: system
    vm_size: Standard_D4s_v3
    node_count: 3
    os_type: Linux
    mode: System
  
  - name: workload
    vm_size: Standard_D8s_v3
    node_count: 3
    min_count: 3
    max_count: 20
    enable_auto_scaling: true
    os_type: Linux
    mode: User

# Networking
network_plugin: azure
network_policy: calico
service_cidr: 10.0.0.0/16
dns_service_ip: 10.0.0.10

# Monitoring
monitoring:
  enabled: true
  log_analytics_workspace_id: /subscriptions/.../workspace-id

# Security
aad_integration: true
rbac_enabled: true
```

### **2. FastAPI Application**

```python
# Especificaciones de recursos por pod
resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "2Gi"
    cpu: "2000m"

# Replicas y autoscaling
replicas:
  min: 3
  max: 20
  target_cpu_utilization: 70%
  target_memory_utilization: 80%

# Healthchecks
liveness_probe:
  http_get:
    path: /health
    port: 8000
  initial_delay_seconds: 30
  period_seconds: 10
  timeout_seconds: 5
  failure_threshold: 3

readiness_probe:
  http_get:
    path: /ready
    port: 8000
  initial_delay_seconds: 10
  period_seconds: 5
  timeout_seconds: 3
  failure_threshold: 3

# Environment variables
env:
  - name: MODEL_PATH
    value: "/models/ensemble_v2"
  - name: FEATURE_STORE_URL
    valueFrom:
      secretKeyRef:
        name: app-secrets
        key: feature-store-url
  - name: LOG_LEVEL
    value: "INFO"
```

### **3. Databricks Cluster**

```json
{
  "cluster_name": "ml-training-gpu",
  "spark_version": "13.3.x-gpu-ml-scala2.12",
  "node_type_id": "Standard_NC6s_v3",
  "driver_node_type_id": "Standard_NC6s_v3",
  "num_workers": 2,
  "autoscale": {
    "min_workers": 2,
    "max_workers": 8
  },
  "auto_termination_minutes": 30,
  "spark_conf": {
    "spark.databricks.delta.preview.enabled": "true",
    "spark.sql.adaptive.enabled": "true"
  },
  "custom_tags": {
    "project": "medical-diagnosis",
    "environment": "production"
  },
  "init_scripts": [
    {
      "dbfs": {
        "destination": "dbfs:/init-scripts/install-mlflow.sh"
      }
    }
  ]
}
```

### **4. Feature Store**

```python
# Feature Store configuration
feature_store_config = {
    "online_store": {
        "type": "redis",
        "host": "medical-features-cache.redis.cache.windows.net",
        "port": 6380,
        "ssl": True,
        "ttl": 3600  # 1 hour
    },
    "offline_store": {
        "type": "delta",
        "database": "feature_store_db",
        "tables": {
            "patient_features": "feature_store_db.patient_features",
            "symptom_features": "feature_store_db.symptom_features"
        }
    },
    "feature_groups": {
        "common_diseases": {
            "features": 150,
            "update_frequency": "daily"
        },
        "rare_diseases": {
            "features": 50,
            "update_frequency": "weekly"
        }
    }
}
```

---

## 🔐 Matriz de Seguridad

### **Control de Acceso (RBAC)**

| Rol                     | Permisos                                                                                                                          | Recursos accesibles                                                                                           |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| **MedicoGeneral** | • Hacer predicciones`<br>`• Ver explicaciones`<br>`• Enviar feedback                                                       | • API `/predict<br>`• API `/explain<br>`• API `/feedback`                                            |
| **MedicoAdmin**   | Todo de MedicoGeneral +`<br>`• Ver métricas agregadas`<br>`• Acceso a dashboards`<br>`• Ver históricos                 | • Grafana dashboards`<br>`• Cosmos DB (read-only)`<br>`• Application Insights                          |
| **DataScientist** | Todo de MedicoAdmin +`<br>`• Acceso a MLflow`<br>`• Entrenar modelos`<br>`• Desplegar a Staging                          | • Databricks workspace`<br>`• MLflow experiments`<br>`• Feature Store`<br>`• Data Lake (read/write) |
| **MLOpsEngineer** | Todo de DataScientist +`<br>`• Desplegar a Producción`<br>`• Gestionar infraestructura`<br>`• Modificar pipelines CI/CD | • AKS clusters`<br>`• Azure DevOps`<br>`• Terraform state`<br>`• Key Vault                          |
| **Auditor**       | Solo lectura de:`<br>`• Logs de predicciones`<br>`• Audit trails`<br>`• Model registry history                           | • Log Analytics`<br>`• MLflow (read-only)`<br>`• Cosmos DB audit logs                                  |

---

## 📈 Métricas y SLOs

### **Service Level Objectives (SLOs)**

| Métrica                    | SLO          | Medición            | Consecuencia si se viola     |
| --------------------------- | ------------ | -------------------- | ---------------------------- |
| **Disponibilidad**    | 99.9% uptime | Azure Monitor        | Alerta critical → PagerDuty |
| **Latencia p50**      | <200ms       | Prometheus histogram | Alerta warning               |
| **Latencia p95**      | <400ms       | Prometheus histogram | Alerta warning               |
| **Latencia p99**      | <500ms       | Prometheus histogram | Alerta critical              |
| **Tasa de error**     | <0.1%        | Application Insights | Alerta critical              |
| **Data drift**        | Score <0.5   | Evidently AI (daily) | Alerta warning               |
| **Model F1**          | >0.85        | MLflow tracking      | Trigger reentrenamiento      |
| **Recall (crítico)** | >0.95        | MLflow tracking      | Alerta critical              |

---

## 🎯Plan de Incident Response

### **Niveles de Severidad**

#### **🔴 Severity 1: Critical (P1)**

- **Definición:** Sistema completamente caído o produciendo predicciones peligrosamente incorrectas
- **Ejemplos:**
  - API devuelve 500 para >50% de requests
  - Recall de casos críticos cae <80%
  - Data breach detectado
- **Respuesta:**
  - PagerDuty notifica a on-call engineer (llamada telefónica)
  - War room en Microsoft Teams en 15 minutos
  - Rollback automático a última versión estable
  - Comunicación a stakeholders en 30 minutos
- **SLA de resolución:** 1 hora

#### **🟡 Severity 2: High (P2)**

- **Definición:** Funcionalidad degradada pero sistema operativo
- **Ejemplos:**
  - Latencia p99 >1 segundo
  - Data drift score >0.7
  - Tasa de error 0.1% - 1%
- **Respuesta:**
  - Slack notification al equipo ML
  - Investigación en 1 hora
  - Plan de mitigación en 4 horas
- **SLA de resolución:** 8 horas

#### **🟢 Severity 3: Medium (P3)**

- **Definición:** Problema menor con workaround disponible
- **Ejemplos:**
  - Dashboard no carga
  - Feedback submission falla ocasionalmente
- **Respuesta:**
  - Ticket en Azure DevOps
  - Priorizado en siguiente sprint
- **SLA de resolución:** 7 días

---

## 📚 Decisiones de Diseño Clave

### **1. ¿Por qué Azure en vez de AWS/GCP?**

**Decisión:** Azure

**Justificación:**

- ✅ **Healthcare focus:** Azure es líder en compliance (HIPAA, HITRUST)
- ✅ **Databricks integration:** Integración nativa y profunda
- ✅ **Región de operación:** Si la solución es para Latinoamérica, Azure tiene buena presencia
- ✅ **Soporte empresarial:** Microsoft tiene programas específicos para healthcare

**Trade-offs:**

- ⚠️ Vendor lock-in (mitigado con contenedores y estándares abiertos)
- ⚠️ Curva de aprendizaje si el equipo conoce más AWS

---

### **2. ¿Por qué Kubernetes en vez de serverless?**

**Decisión:** AKS (Kubernetes)

**Justificación:**

- ✅ **Control granular:** Necesario para latencias predecibles en salud
- ✅ **Escalabilidad horizontal:** 3-20 pods según demanda
- ✅ **Estado:** Modelos cargados en memoria (warm start)
- ✅ **Cost-effective:** Para cargas sostenidas, K8s es más barato que serverless

**Alternativa considerada:** Azure Functions

- ✅ Más simple de mantener
- ❌ Cold starts (latencia impredecible)
- ❌ Más costoso para alto throughput
- ❌ Menos control sobre recursos

**Decisión híbrida:** Usar AKS para API principal + Azure Functions para batch

---

### **3. ¿Por qué MLflow en vez de alternativas propietarias?**

**Decisión:** MLflow

**Justificación:**

- ✅ **Open source:** No vendor lock-in
- ✅ **Estándar de facto:** Gran comunidad y adopción
- ✅ **Integración Databricks:** Nativo en Databricks
- ✅ **Model registry robusto:** Staging/Production/Archived

**Alternativas consideradas:**

- Weights & Biases: ✅ UI mejor ❌ Vendor lock-in ❌ Costo
- Neptune.ai: ✅ Buenas features ❌ Vendor lock-in ❌ Costo
- Azure ML SDK: ✅ Nativo Azure ❌ Lock-in fuerte ❌ Menos flexible

---

## ✅ Checklist de Implementación

### **Fase 1: Setup Inicial (Semana 1-2)**

-  Crear subscripción Azure
-  Setup de Terraform para IaC
-  Crear resource groups
-  Configurar networking (VNet, subnets)
-  Setup Azure Active Directory (users, groups, apps)
-  Crear Key Vault y agregar secretos
-  Setup Azure DevOps project

### **Fase 2: Data Infrastructure (Semana 2-3)**

-  Crear Azure Data Lake Storage Gen2
-  Setup de Azure Data Factory
-  Configurar Databricks workspace
-  Crear Delta Lake database structure
-  Setup Feature Store
-  Implementar ingesta de datos de prueba
-  Configurar Great Expectations

### **Fase 3: ML Pipeline (Semana 3-5)**

-  Setup MLflow tracking server
-  Desarrollar notebooks de entrenamiento
-  Implementar modelos base (comunes + huérfanas)
-  Setup de hyperparameter tuning con Optuna
-  Crear ensemble model
-  Implementar tests de validación
-  Registrar modelos en MLflow Registry

### **Fase 4: API y Despliegue (Semana 5-7)**

-  Desarrollar FastAPI application
-  Implementar endpoints (/predict, /health, etc.)
-  Agregar SHAP explainability
-  Crear Dockerfile optimizado
-  Setup Azure Container Registry
-  Crear AKS cluster
-  Configurar NGINX Ingress
-  Configurar HPA (autoscaling)
-  Setup Azure API Management

### **Fase 5: CI/CD (Semana 7-8)**

-  Crear Azure Pipeline para CI
-  Implementar tests automatizados (pytest)
-  Crear pipeline de CD (staging + production)
-  Configurar A/B testing automation
-  Setup de rollback automático
-  Implementar health checks en pipeline

### **Fase 6: Monitoreo (Semana 8-9)**

-  Instalar Prometheus en AKS
-  Configurar Grafana dashboards
-  Setup Application Insights
-  Implementar Evidently AI drift detection
-  Configurar Azure Log Analytics
-  Setup PagerDuty para alertas
-  Crear runbooks para incidentes comunes

### **Fase 7: Seguridad (Semana 9-10)**

-  Configurar Azure Policy para HIPAA
-  Implementar audit logging completo
-  Setup de encriptación at-rest y in-transit
-  Configurar RBAC en AKS y Databricks
-  Penetration testing
-  Security audit externo

### **Fase 8: Testing y Validación (Semana 10-11)**

-  Testing end-to-end completo
-  Load testing (>100 req/s)
-  Validación con médicos especialistas
-  Testing de disaster recovery
-  Validación de SLOs
-  Documentation review

### **Fase 9: Piloto (Semana 12-16)**

-  Deployment en 2-3 hospitales piloto
-  Training de médicos usuarios
-  Recolección de feedback
-  Iteraciones basadas en feedback
-  Validación de métricas de negocio

### **Fase 10: Producción (Semana 17+)**

-  Go-live en producción
-  Monitoreo 24/7 primera semana
-  Plan de escalado nacional
-  Mejora continua

---

*Proyecto desarrollado por Felipe Guerra y Mavelyn Sterling para el Taller #3 de MLOps - Maestría en Inteligencia Artificial Aplicada*

*Versión 2.0 - Pipeline MLOps End-to-End de Nivel Empresarial*
